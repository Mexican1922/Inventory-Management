import React from "react";
interface CreateOrderDialogProps {
    open: boolean;
    onOpenChange: (open: boolean) => void;
}
export declare const CreateOrderDialog: React.FC<CreateOrderDialogProps>;
export {};
